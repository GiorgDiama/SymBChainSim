dynamic_sim:
  use: False
  config: "Configs/dynamic_config.yaml"

simulation:
  init_CP: "PBFT"
  simTime: 300
  stop_after_blocks: -1
  interval_switch: False
  interval_mean: 60
  interval_sigma: 10
  debugging_mode: False
  remove_timeouts: False
  print_every: 200
  snapshots: False
  print_info: False

application:
  Nn: 16
  TI_dur: 25
  Tn: 10_000
  base_transation_size: 0
  Tsize: 0.001
  transaction_model: "global"

execution:
  creation_time: 0.1
  block_val_delay: 0.025
  msg_val_delay: 0.01
  sync_message_request_delay: 1
  proposer_selection: "round_robin"

data:
  Bsize: 1
  base_block_size: 0
  block_interval: 0

network:
  base_msg_size: 0.2
  gossip: False
  num_neighbours: 3
  use_latency: distance
  same_city_latency_ms: 10
  same_city_dev_ms: 0
  queueing_delay: 0
  processing_delay: 0

  bandwidth:
    mean: 1
    dev: 0
    min: 2

  measure_avg_transmission_delay: False

consensus:
  BigFoot: Chain/Consensus/BigFoot/BigFoot_config.yaml
  PBFT: Chain/Consensus/PBFT/PBFT_config.yaml

behaviour:
  use: False
  config: "Configs/behaviour_config.yaml"
